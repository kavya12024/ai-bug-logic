# Dockerfile for Node.js execution
FROM node:18-alpine

WORKDIR /app

# Add empty handler for code injection
RUN echo 'console.log("Node.js container ready");' > /app/run.js

CMD ["node", "/app/run.js"]
